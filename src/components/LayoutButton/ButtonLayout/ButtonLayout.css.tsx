import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      justifyContent: 'flex-start',
      gap:16,
      margin: 10,
    },
    containerButtons: {
      flexDirection: 'row',
      gap:16
    },
  })